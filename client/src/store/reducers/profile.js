import {} from "../actions/types";

const initialState = {
  profiles: [],
  profile: {},
  loading: false
};

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
